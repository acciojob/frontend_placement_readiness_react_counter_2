import React, { Component } from "react";

class CounterApp extends Component {
  render() {
    return (
      <div>
        <h1>Counter App</h1>
        <p>Count:0</p>
        <button>Count</button>
        <button>Reset</button>
      </div>
    );
  }
}

export default CounterApp;
